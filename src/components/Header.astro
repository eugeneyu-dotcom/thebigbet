---
import HeaderLink from './HeaderLink.astro';
import LanguagePicker from './LanguagePicker.astro';
import { getLangFromUrl, useTranslations } from '../i18n/ui';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<header class="bg-slate-950 border-b border-slate-800 sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <div class="flex">
        <div class="flex-shrink-0 flex items-center">
          <a href={`/${lang}/`} class="text-2xl font-black text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-yellow-600 italic tracking-tighter pr-1">
            THE BIG BET
          </a>
        </div>
        <nav class="hidden sm:ml-8 sm:flex sm:space-x-8">
          <HeaderLink href={`/${lang}/`} class="inline-flex items-center px-1 pt-1 text-sm font-bold text-slate-100 hover:text-amber-300 transition-colors duration-200">
            {t('nav.home')}
          </HeaderLink>
          <HeaderLink href={`/${lang}/schedule`} class="inline-flex items-center px-1 pt-1 text-sm font-bold text-slate-400 hover:text-amber-300 transition-colors duration-200">
            {t('nav.schedule')}
          </HeaderLink>
          <HeaderLink href={`/${lang}/analysis`} class="inline-flex items-center px-1 pt-1 text-sm font-bold text-slate-400 hover:text-amber-300 transition-colors duration-200">
            {t('nav.analysis')}
          </HeaderLink>
        </nav>
      </div>
      <div class="flex items-center">
        <LanguagePicker />
      </div>
    </div>
  </div>
</header>
